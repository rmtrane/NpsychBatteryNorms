# Get Race Group for Standardized Scores

Get race group for finding standardized scores.

## Usage

``` r
get_race_group(race)
```

## Arguments

- race:

  numeric vector with race (according to NACC; see `rdd$RACE` for
  details) to be converted to groups
