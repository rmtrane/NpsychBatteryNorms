# Default Standardization Methods

Default Standardization Methods

## Usage

``` r
default_methods
```

## Format

A named list. Each entry is named for a neuropsych score, and contains a
named vector with two entires: `method` gives the default method, and
`version` the default version.
